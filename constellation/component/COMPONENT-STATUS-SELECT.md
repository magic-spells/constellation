---
name: StatusSelect
kind: ui
status: built
code_refs:
  - viewer/app/components/StatusSelect.pzl
---

Card status picker (`planned` → `building` → `built` → `verified`, plus *none*); PATCHes through the write API ([[FILE-SERVE]]). Built on the puzzle-pieces `select` piece — see [[DECISION-VIEWER-FRAMEWORK]].
